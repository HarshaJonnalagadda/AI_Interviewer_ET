# AI service — Python/FastAPI service (see /ai-service)
AI_SERVICE_URL=http://localhost:8000
AI_SERVICE_API_KEY=

# Auth — email OTP
RESEND_API_KEY=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Auth — session cookie signing (generate with: openssl rand -hex 32)
SESSION_SECRET=

# Auth — email address admin OTPs are sent from (must be verified in Resend)
OTP_FROM_EMAIL=StarCanvas <login@yourdomain.com>

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
